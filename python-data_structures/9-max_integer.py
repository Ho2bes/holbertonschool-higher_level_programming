#!/usr/bin/python3
def max_integer(my_list=[]):
    if not liste:
        return None
    max_element = liste[0]
    for element in liste[1:]:
        if element > max_element:
            max_element = element
            return max_element
